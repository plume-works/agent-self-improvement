services:
  mcp-gateway:
    image: docker/mcp-gateway
    pull_policy: if_not_present
    restart: unless-stopped
    profiles: ['mcp']
    environment:
      DOCKER_SECRETS_ENGINE_SOCKET: ${DOCKER_SECRETS_ENGINE_SOCKET:-/root/.cache/docker-secrets-engine/engine.sock}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOST_MCP_DIR:-/tmp/stub-mcp}:${CONTAINER_MCP_DIR:-/tmp/stub-mcp}:ro
      - ${HOST_SECRETS_SOCKET:-/tmp/stub-secrets-engine.sock}:${DOCKER_SECRETS_ENGINE_SOCKET:-/tmp/stub-secrets-engine.sock}:ro
    # On a freshly (re)created container, Docker Desktop's per-container DNS proxy can take
    # a moment to attach. The gateway's image-signature check does an outbound DNS lookup
    # immediately on startup, and if it loses that race it exits; in-place restarts don't
    # reliably re-trigger the DNS attach, so `restart: unless-stopped` alone can crash-loop
    # forever. Wait for DNS to actually work before handing off to the real entrypoint.
    entrypoint:
      - sh
      - -c
      - |
        for i in $$(seq 1 30); do
          nslookup registry-1.docker.io >/dev/null 2>&1 && break
          echo "mcp-gateway: waiting for network/DNS to be ready ($$i/30)..." >&2
          sleep 1
        done
        exec /docker-mcp gateway run "$$@"
      - sh
    command:
      # Docker Desktop's MCP Toolkit stores configured servers as a "profile" in
      # ~/.docker/mcp/mcp-toolkit.db (mirrors `docker mcp gateway run --profile default`).
      # The legacy --catalog/--config/--registry/--tools-config flags read stale/empty
      # YAML files and also require absolute paths to resolve under the container's
      # $HOME/.docker/mcp, which ${CONTAINER_MCP_DIR} (mounted to match HOME=/root) satisfies.
      - --profile=${MCP_GATEWAY_PROFILE:-default}
      # docker-desktop secrets provider: requires Docker Desktop on the host.
      # The mcp profile only activates when ~/.docker/mcp exists, which Docker Desktop manages.
      - --secrets=docker-desktop
      - --watch=true
      - --transport=sse
      - --port=8811
      # The gateway's bearer-token auth guards against DNS-rebinding attacks from
      # browser clients hitting a localhost-bound port. The devcontainer instead reaches
      # this service over the private compose network by service name (mcp-gateway:8811).
      # It intentionally has no host-published port: every worktree has its own sidecar,
      # and publishing a fixed host port would make a clean startup fail whenever another
      # worktree's gateway is already running. The token is also regenerated per container
      # start, so it can't be pinned in the git-tracked .mcp.json without breaking on
      # every restart.
      - --allow-unauthenticated

  devcontainer:
    # Tag-only fallback for standalone `docker compose -f .devcontainer/docker-compose.yml`
    # use. The digest pin lives in ../compose.pins.yml, which devcontainer.json layers on
    # top of this file; pass `-f compose.pins.yml` too when invoking compose directly.
    image: ghcr.io/plume-works/agent-desktop:edge
    # Required for Docker in Docker (dind) to allow nested containers, and for
    # privileged access to the host's GPU for hardware-accelerated rendering
    # through Xpra/VirtualGL.
    # Note: privileged mode grants the container elevated permissions, which can pose
    # security risks. Use with caution and only in trusted environments.
    privileged: true

    environment:
      DOCKER_SECRETS_ENGINE_SOCKET: ${DOCKER_SECRETS_ENGINE_SOCKET:-/root/.cache/docker-secrets-engine/engine.sock}

    extra_hosts:
      - host.docker.internal:host-gateway

    volumes:
      # Mount the git common dir at its host path so `git worktree` checkouts can
      # resolve their .git file back to the main repository.
      - ${GIT_REPO:-$PWD}:${GIT_REPO:-$PWD}:rw
      - ${LOCAL_WORKSPACE_FOLDER:-$PWD}:${LOCAL_WORKSPACE_FOLDER:-$PWD}:rw

      # The workspace itself, at the path devcontainer.json declares as workspaceFolder.
      - ${LOCAL_WORKSPACE_FOLDER:-$PWD}:/workspaces/${WORKSPACE_BASENAME:-project}:rw

      # Docker mcp setup from host
      - ${HOST_MCP_DIR:-/tmp/stub-mcp}:${CONTAINER_MCP_DIR:-/tmp/stub-mcp}:ro
      - ${HOST_SECRETS_SOCKET:-/tmp/stub-secrets-engine.sock}:${DOCKER_SECRETS_ENGINE_SOCKET:-/tmp/stub-secrets-engine.sock}:ro

      # Agent auth/config state, shared across all devcontainer instances (worktrees,
      # Codespaces, etc). Declared with an explicit top-level `name:` below so Compose
      # uses the literal volume name instead of prefixing it with the per-project name,
      # which would otherwise isolate each devcontainer instance's auth state.
      #
      # ~/.claude.json is a single file, not a directory, and Docker named volumes are
      # always directory-backed (mounting one straight at a file path materializes an
      # empty directory there instead, breaking Claude Code's read/write of that file).
      # It's persisted as a plain file inside the agentdev-claude volume instead, with
      # .devcontainer/scripts/postCreateCommand.sh symlinking ~/.claude.json to it.
      - agentdev-claude:/root/.claude
      - agentdev-codex:/root/.codex

    command: ['sleep', 'infinity']

volumes:
  # `name:` pins the literal Docker volume name so it's shared across every
  # devcontainer instance (worktrees, Codespaces, etc), instead of Compose's
  # default per-project name prefix isolating each instance's auth state.
  agentdev-claude:
    name: agentdev-claude
    external: true
  agentdev-codex:
    name: agentdev-codex
    external: true
