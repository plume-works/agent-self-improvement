# Aligned with the Prettier pass that Super-Linter runs, which owns wrapping.
default: true
MD013: false # line length — Prettier owns formatting
MD024:
  siblings_only: true # duplicate headings allowed in different sections
MD033: false # inline HTML is used for validator directives
MD041: false # first line need not be a top-level heading (frontmatter)
