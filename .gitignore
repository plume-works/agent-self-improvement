# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Build outputs
/dist/
/coverage/

# Local development
.env
.env.*
!.env.example
.DS_Store
.vscode/
.idea/
*.log

# Runtime state must never enter the repository
.self-improvement/
candidates/
proposals/
authorizations/
backups/
archive/
logs/
locks/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Test scratch. Every live run gets its own directory under test-runs/ and is
# left there to be read afterwards; /tmp/ is where they used to go, and stays
# ignored until `make clean` clears whatever is still sitting in it.
/test-runs/
/tmp/
